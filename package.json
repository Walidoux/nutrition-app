{
  "name": "personal-ios-capacitor-stack",
  "private": true,
  "workspaces": [
    "api",
    "mobile"
  ],
  "scripts": {
    "dev:api": "bun --cwd api run dev",
    "dev:mobile": "bun --cwd mobile run dev",
    "start:api": "bun --cwd api run start",
    "build:mobile": "bun --cwd mobile run build"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide"
  ]
}
